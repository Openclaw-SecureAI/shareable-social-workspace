# Structure

Canonical root:

`<YOUR_OPENCLAW_WORKSPACE>/social-workspace-integration`

Key directories:
- `brand/`
- `ideas/`
- `drafts/`
- `outputs/`
- `templates/`
- `config/`
- `logs/`
- `schema/`

Principles:
- one source of truth for drafts and approvals
- separate strategy from execution artifacts
- keep external publishing services outside the core draft model
